class Question {
  String questionText;
  bool questionAnswer;

  // Constructor with required keyword for named parameters
  Question({required this.questionText, required this.questionAnswer});
}

// Question newQuestion = Question(questionText: 'text', questionAnswer: true);
